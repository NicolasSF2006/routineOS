diff --git a/src/features/routine-builder/components/routine-builder-view.tsx b/src/features/routine-builder/components/routine-builder-view.tsx
index 11fdcec..28e8082 100644
--- a/src/features/routine-builder/components/routine-builder-view.tsx
+++ b/src/features/routine-builder/components/routine-builder-view.tsx
@@ -465,6 +465,7 @@ export function RoutineBuilderView({ onBackToSettings }: RoutineBuilderViewProps
   const selectedDate = parseDateKey(selectedDateKey)
   const selectedWeekday = getWeekdayFromDateKey(selectedDateKey)
   const weekDays = useMemo(() => buildWeekDays(selectedDate), [selectedDateKey])
+  const selectedWeekDayInfo = weekDays.find((day) => day.dateKey === selectedDateKey) ?? weekDays[getWeekdayIndex(selectedWeekday)]
   const selectedMonthLabel = getMonthLabel(selectedDate.getFullYear(), selectedDate.getMonth())
   const activeDraftRoutine = draftRoutine ?? routine
   const startTime = getRoutineStartTime(activeDraftRoutine)
@@ -1892,28 +1893,6 @@ export function RoutineBuilderView({ onBackToSettings }: RoutineBuilderViewProps
               </h1>
 
               <div className="flex shrink-0 items-center gap-1">
-                <Button
-                  type="button"
-                  variant="ghost"
-                  size="icon"
-                  className="size-8 rounded-full"
-                  onClick={() => moveDay(-1)}
-                  aria-label="Dia anterior"
-                  title="Dia anterior"
-                >
-                  <ChevronLeft className="size-4" />
-                </Button>
-                <Button
-                  type="button"
-                  variant="ghost"
-                  size="icon"
-                  className="size-8 rounded-full"
-                  onClick={() => moveDay(1)}
-                  aria-label="Próximo dia"
-                  title="Próximo dia"
-                >
-                  <ChevronRight className="size-4" />
-                </Button>
                 <Button
                   type="button"
                   variant="outline"
@@ -1957,54 +1936,38 @@ export function RoutineBuilderView({ onBackToSettings }: RoutineBuilderViewProps
           </header>
 
           <div className="border-b border-border/60 bg-background px-3 py-3 sm:px-5">
-            <div className="flex gap-2 overflow-x-auto pb-1 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">
-              {weekDays.map((day) => {
-                const isSelected = day.dateKey === selectedDateKey
-
-                return (
-                  <button
-                    key={day.dateKey}
-                    type="button"
-                    onClick={() => selectDate(day.dateKey)}
-                    className={cn(
-                      "flex min-w-14 flex-col items-center justify-center rounded-2xl border px-3 py-2 text-center transition-colors",
-                      isSelected
-                        ? "border-cyan-400/80 bg-cyan-400/15 text-cyan-950 shadow-sm shadow-cyan-950/20 dark:text-cyan-100"
-                        : "border-border/70 bg-muted/20 text-muted-foreground hover:bg-muted",
-                      day.isToday && !isSelected && "border-cyan-400/40 text-foreground",
-                    )}
-                    aria-pressed={isSelected}
-                  >
-                    <span className="text-xs font-medium leading-none">
-                      {day.short.toLowerCase()}
-                    </span>
-                    <span className="mt-1 font-mono text-lg font-semibold leading-none tabular-nums">
-                      {day.dayNumber}
-                    </span>
-                  </button>
-                )
-              })}
-            </div>
+            <div className="flex items-center gap-3">
+              <div
+                className={cn(
+                  "flex size-16 shrink-0 flex-col items-center justify-center rounded-2xl border text-center shadow-sm",
+                  selectedWeekDayInfo.isToday
+                    ? "border-cyan-400/80 bg-cyan-400/15 text-cyan-950 shadow-cyan-950/20 dark:text-cyan-100"
+                    : "border-border/70 bg-muted/20 text-foreground",
+                )}
+                aria-label={`Dia atual: ${selectedWeekDayInfo.label} ${selectedWeekDayInfo.dayNumber}`}
+              >
+                <span className="text-xs font-medium leading-none">
+                  {selectedWeekDayInfo.short.toLowerCase()}
+                </span>
+                <span className="mt-1 font-mono text-2xl font-semibold leading-none tabular-nums">
+                  {selectedWeekDayInfo.dayNumber}
+                </span>
+              </div>
 
-            <div className="mt-3 flex min-h-9 items-center justify-between gap-3">
               <div className="min-w-0 flex-1">
-                {conflictMessage ? (
-                  <p className="flex items-start gap-2 text-sm font-medium leading-5 text-red-300">
-                    <AlertTriangle className="mt-0.5 size-4 shrink-0" />
-                    <span className="min-w-0">{conflictMessage}</span>
-                  </p>
-                ) : null}
+                <p className="truncate text-sm font-medium text-muted-foreground">Dia atual</p>
+                <p className="truncate text-lg font-semibold text-foreground">
+                  {selectedWeekDayInfo.label}, {selectedWeekDayInfo.dayNumber}
+                </p>
               </div>
-              <Button
-                type="button"
-                variant="outline"
-                size="sm"
-                className="h-9 rounded-full px-3"
-                onClick={clearCurrentMonthRoutine}
-              >
-                Limpar
-              </Button>
             </div>
+
+            {conflictMessage ? (
+              <p className="mt-3 flex items-start gap-2 text-sm font-medium leading-5 text-red-300">
+                <AlertTriangle className="mt-0.5 size-4 shrink-0" />
+                <span className="min-w-0">{conflictMessage}</span>
+              </p>
+            ) : null}
           </div>
 
           <div
