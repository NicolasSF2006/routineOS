--- a/src/features/routine-builder/components/routine-builder-view.tsx	2026-07-09 16:10:02.215115752 +0000
+++ b/src/features/routine-builder/components/routine-builder-view.tsx	2026-07-09 16:28:06.285576111 +0000
@@ -8,7 +8,6 @@
   Copy,
   MoreHorizontal,
   Pencil,
-  Plus,
   Save,
   Trash2,
   X,
@@ -146,10 +145,11 @@
   }
 }
 
-const HOUR_ROW_HEIGHT = 128
-const MIN_VISIBLE_BLOCK_HEIGHT = 22
+const HOUR_ROW_HEIGHT = 96
+const MIN_VISIBLE_BLOCK_HEIGHT = 20
 const BLOCK_LAYOUT_GAP = 4
-const MIN_TIMELINE_MINUTES = 8 * 60
+const FULL_DAY_START_MINUTES = 0
+const FULL_DAY_END_MINUTES = 24 * 60
 
 interface RoutineBlockLayout {
   block: RoutineBlock
@@ -184,31 +184,17 @@
   return timeToMinutes(block.endTime) ?? getBlockStartMinutes(block, fallbackStartMinutes) + block.durationMinutes
 }
 
-function getTimelineRange(week: RoutineWeek, fallbackStartTime: string) {
-  const fallbackStartMinutes = timeToMinutes(fallbackStartTime) ?? 8 * 60
-  const allBlocks = week.days.flatMap((day) => day.blocks)
-
-  const firstBlockStart = allBlocks.length > 0
-    ? Math.min(...allBlocks.map((block) => getBlockStartMinutes(block, fallbackStartMinutes)))
-    : fallbackStartMinutes
-  const lastBlockEnd = allBlocks.length > 0
-    ? Math.max(...allBlocks.map((block) => getBlockEndMinutes(block, fallbackStartMinutes)))
-    : fallbackStartMinutes + MIN_TIMELINE_MINUTES
-
-  const startMinutes = Math.max(0, Math.floor(firstBlockStart / 60) * 60 - 60)
-  const roundedEndMinutes = Math.ceil(lastBlockEnd / 60) * 60
-  const endMinutes = Math.max(
-    startMinutes + MIN_TIMELINE_MINUTES,
-    roundedEndMinutes + 60,
-  )
-
-  return { startMinutes, endMinutes }
+function getTimelineRange() {
+  return {
+    startMinutes: FULL_DAY_START_MINUTES,
+    endMinutes: FULL_DAY_END_MINUTES,
+  }
 }
 
 function getTimeSlots(startMinutes: number, endMinutes: number): number[] {
   const slots: number[] = []
 
-  for (let minutes = startMinutes; minutes <= endMinutes; minutes += 60) {
+  for (let minutes = startMinutes; minutes < endMinutes; minutes += 60) {
     slots.push(minutes)
   }
 
@@ -261,10 +247,7 @@
   const activeDraftRoutine = draftRoutine ?? routine
   const startTime = getRoutineStartTime(activeDraftRoutine)
   const fallbackStartMinutes = timeToMinutes(startTime) ?? 8 * 60
-  const timelineRange = useMemo(
-    () => getTimelineRange(draftWeek ?? createRoutineWeekFromDate(selectedDateKey, activeDraftRoutine), startTime),
-    [activeDraftRoutine, draftWeek, selectedDateKey, startTime],
-  )
+  const timelineRange = useMemo(() => getTimelineRange(), [])
   const timeSlots = useMemo(
     () => getTimeSlots(timelineRange.startMinutes, timelineRange.endMinutes),
     [timelineRange.endMinutes, timelineRange.startMinutes],
@@ -777,43 +760,39 @@
 
           <div
             ref={workspaceScrollRef}
-            className="overflow-x-auto overscroll-x-contain rounded-[2.25rem] border border-border/50 bg-muted/10 p-4 shadow-sm sm:p-5"
+            className="max-h-[calc(100svh-15rem)] overflow-auto overscroll-contain rounded-[1.75rem] border border-border/50 bg-muted/10 p-3 shadow-sm sm:p-4"
           >
             <div
-              className="routine-builder-scheduler-grid grid min-w-[1320px] items-stretch gap-x-2 gap-y-4 2xl:min-w-0 2xl:gap-x-3"
+              className="routine-builder-scheduler-grid grid min-w-[1240px] items-stretch gap-x-2 gap-y-3 2xl:min-w-0 2xl:gap-x-3"
               style={{
                 ["--routine-builder-timeline-height" as string]: `${timelineHeight}px`,
-                gridTemplateColumns: "88px repeat(7, minmax(165px, 1fr))",
-                gridTemplateRows: `88px ${timelineHeight}px`,
+                gridTemplateColumns: "76px repeat(7, minmax(152px, 1fr))",
+                gridTemplateRows: `64px ${timelineHeight}px`,
               }}
             >
-              <div className="routine-builder-timezone-card flex min-h-[88px] flex-col items-center justify-center rounded-[1.75rem] border border-border/60 bg-background/80 shadow-sm backdrop-blur">
-                <span className="text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">
-                  Horários
-                </span>
-              </div>
+              <div className="min-h-16" aria-hidden="true" />
 
               {weekDays.map((day) => (
                 <div
                   key={`${day.dateKey}-header`}
                   ref={day.key === selectedWeekday ? selectedDayColumnRef : undefined}
                   className={cn(
-                    "routine-builder-day-header flex min-h-[88px] flex-col items-center justify-center gap-1.5 rounded-[1.75rem] border border-border/60 bg-background/80 shadow-sm backdrop-blur",
+                    "routine-builder-day-header flex min-h-16 flex-col items-center justify-center gap-1 rounded-[1.25rem] border border-border/60 bg-background/80 px-3 py-2 shadow-sm backdrop-blur",
                     day.isToday && "routine-builder-day-header-today border-cyan-400/80 bg-cyan-400/10 shadow-cyan-400/10",
                   )}
                 >
-                  <span className="text-sm font-medium text-muted-foreground">{day.label}</span>
-                  <span className="font-mono text-3xl font-semibold leading-none text-foreground tabular-nums">
+                  <span className="text-sm font-medium leading-5 text-muted-foreground">{day.label}</span>
+                  <span className="font-mono text-xl font-semibold leading-none text-foreground tabular-nums">
                     {day.dayNumber}
                   </span>
                 </div>
               ))}
 
-              <div className="routine-builder-time-column relative rounded-[2rem] border border-border/40 bg-background/70 shadow-sm" style={{ height: timelineHeight }}>
+              <div className="routine-builder-time-column relative rounded-[1.5rem] border border-border/40 bg-background/70 shadow-sm" style={{ height: timelineHeight }}>
                 {timeSlots.map((slot) => (
                   <span
                     key={`time-${slot}`}
-                    className="routine-builder-time-label absolute left-0 right-0 -translate-y-1/2 whitespace-nowrap text-center text-xs leading-4 text-muted-foreground"
+                    className="routine-builder-time-label absolute left-0 right-0 whitespace-nowrap text-center text-sm leading-5 text-muted-foreground"
                     style={{ top: minutesToPixels(slot - timelineRange.startMinutes) }}
                   >
                     {formatTimeLabel(slot)}
@@ -823,12 +802,6 @@
 
               {weekDays.map((day) => {
                 const layouts = dayBlockLayouts.get(day.key) ?? []
-                const lastLayout = layouts.at(-1)
-                const addButtonTop = Math.min(
-                  Math.max(24, timelineHeight - 56),
-                  Math.max(24, (lastLayout ? lastLayout.top + lastLayout.height + 14 : 24)),
-                )
-
                 return (
                   <div
                     key={`${day.dateKey}-lane`}
@@ -850,21 +823,6 @@
                       />
                     ))}
 
-                    <button
-                      type="button"
-                      onClick={() => openCreateDialog("study", day.key)}
-                      onDragOver={(event) => handleDayDragOver(event, day.key)}
-                      onDrop={(event) => handleDrop(event, day.key)}
-                      className={cn(
-                        "routine-builder-add-slot absolute left-2 right-2 z-10 flex min-h-11 items-center justify-center rounded-2xl border border-dashed border-border/70 bg-background/70 text-muted-foreground transition hover:border-primary/70 hover:bg-primary/10 hover:text-primary",
-                        layouts.length > 0 && "opacity-0 group-hover/day-lane:opacity-100 focus-visible:opacity-100",
-                      )}
-                      style={{ top: addButtonTop }}
-                      aria-label={`Adicionar bloco em ${day.label}`}
-                    >
-                      <Plus className="size-4" />
-                    </button>
-
                     {layouts.map(({ block, top, height }, blockIndex) => {
                       const option = getBuilderBlockOption(block.type)
                       const menuOpen = openMenuBlockId === block.id
@@ -892,14 +850,14 @@
                             <div className="flex min-w-0 items-start justify-between gap-2">
                               <div className="min-w-0 flex-1">
                                 {!isCompactBlock ? (
-                                  <span className="mb-1 block text-[0.72rem] leading-4 text-muted-foreground">
+                                  <span className="mb-1 block text-sm leading-5 text-muted-foreground">
                                     {block.startTime} – {block.endTime} • {block.durationMinutes}min
                                   </span>
                                 ) : null}
                                 <span
                                   className={cn(
                                     "wrap-break-word block whitespace-normal font-semibold text-foreground",
-                                    isCompactBlock ? "truncate text-xs leading-tight" : "text-sm leading-snug sm:text-base",
+                                    isCompactBlock ? "truncate text-sm leading-tight" : "text-base leading-snug",
                                   )}
                                 >
                                   {block.title}
--- a/src/styles/globals.css	2026-07-09 16:10:02.216623095 +0000
+++ b/src/styles/globals.css	2026-07-09 16:28:22.138147696 +0000
@@ -153,19 +153,18 @@
 
 @layer components {
   .routine-builder-scheduler-grid {
-    min-width: 1320px;
+    min-width: 1240px;
     display: grid;
-    grid-template-columns: 88px repeat(7, minmax(165px, 1fr));
-    grid-template-rows: 88px var(--routine-builder-timeline-height);
+    grid-template-columns: 76px repeat(7, minmax(152px, 1fr));
+    grid-template-rows: 64px var(--routine-builder-timeline-height);
     column-gap: 0.5rem;
-    row-gap: 1rem;
+    row-gap: 0.75rem;
     align-items: stretch;
   }
 
-  .routine-builder-timezone-card,
   .routine-builder-day-header {
     display: flex;
-    min-height: 5.5rem;
+    min-height: 4rem;
     flex-direction: column;
     align-items: center;
     justify-content: center;
@@ -175,13 +174,9 @@
     backdrop-filter: blur(14px);
   }
 
-  .routine-builder-timezone-card {
-    border-radius: 1.75rem;
-  }
-
   .routine-builder-day-header {
-    gap: 0.35rem;
-    border-radius: 1.75rem;
+    gap: 0.25rem;
+    border-radius: 1.25rem;
   }
 
   .routine-builder-day-header-today {
@@ -197,7 +192,7 @@
   }
 
   .routine-builder-time-column {
-    border-radius: 2rem;
+    border-radius: 1.5rem;
     border: 1px solid color-mix(in oklch, var(--border) 35%, transparent);
     background: color-mix(in oklch, var(--background) 70%, transparent);
     box-shadow: 0 18px 45px color-mix(in oklch, var(--foreground) 4%, transparent);
@@ -208,9 +203,9 @@
     left: 0;
     right: 0;
     text-align: center;
-    transform: translateY(-50%);
-    font-size: 0.75rem;
-    line-height: 1rem;
+    transform: none;
+    font-size: 0.875rem;
+    line-height: 1.25rem;
     color: var(--muted-foreground);
     white-space: nowrap;
   }
@@ -232,27 +227,6 @@
     border-top: 1px solid color-mix(in oklch, var(--border) 46%, transparent);
   }
 
-  .routine-builder-add-slot {
-    position: absolute;
-    left: 0.55rem;
-    right: 0.55rem;
-    z-index: 2;
-    display: flex;
-    min-height: 2.75rem;
-    align-items: center;
-    justify-content: center;
-    border-radius: 1rem;
-    border: 1px dashed color-mix(in oklch, var(--border) 70%, transparent);
-    background: color-mix(in oklch, var(--background) 55%, transparent);
-    color: var(--muted-foreground);
-    transition: border-color 160ms ease, background-color 160ms ease, color 160ms ease;
-  }
-
-  .routine-builder-add-slot:hover {
-    border-color: color-mix(in oklch, var(--primary) 75%, transparent);
-    background: color-mix(in oklch, var(--primary) 8%, transparent);
-    color: var(--primary);
-  }
 
   .routine-builder-schedule-block {
     position: absolute;
@@ -268,21 +242,21 @@
 
   @media (min-width: 640px) {
     .routine-builder-scheduler-grid {
-      min-width: 1320px;
-      grid-template-columns: 88px repeat(7, minmax(165px, 1fr));
-      grid-template-rows: 88px var(--routine-builder-timeline-height);
+      min-width: 1240px;
+      grid-template-columns: 76px repeat(7, minmax(152px, 1fr));
+      grid-template-rows: 64px var(--routine-builder-timeline-height);
       column-gap: 0.5rem;
-      row-gap: 1rem;
+      row-gap: 0.75rem;
     }
   }
 
   @media (min-width: 1440px) {
     .routine-builder-scheduler-grid {
       min-width: 0;
-      grid-template-columns: 88px repeat(7, minmax(0, 1fr));
-      grid-template-rows: 88px var(--routine-builder-timeline-height);
+      grid-template-columns: 76px repeat(7, minmax(0, 1fr));
+      grid-template-rows: 64px var(--routine-builder-timeline-height);
       column-gap: 0.75rem;
-      row-gap: 1rem;
+      row-gap: 0.75rem;
     }
 
     .routine-builder-block-panel {
